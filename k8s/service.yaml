apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: default
  labels:
    app: sample-app
spec:
  type: NodePort
  selector:
    app: sample-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500
      protocol: TCP

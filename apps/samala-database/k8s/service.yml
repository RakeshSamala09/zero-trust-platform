apiVersion: v1
kind: Service
metadata:
  name: samala-database
spec:
  type: ClusterIP
  selector:
    app: samala-database
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
